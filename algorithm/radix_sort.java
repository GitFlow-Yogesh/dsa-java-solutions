Very easy steps- Units place → group and sort by last digit
            Tens place → group again based on second-last digit
            Hundreds → and so on...
Defination = Radix Sort is a non-comparison-based sorting algorithm that sorts numbers digit by digit, from the least significant digit (LSD) to the most significant digit (MSD).
steps : find max first then 
  lets do code :
  
